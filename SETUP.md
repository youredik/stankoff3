# 🚀 Инструкция по установке Stankoff Portal

## Шаг 1: Проверка предварительных требований

Убедитесь, что установлены:

```bash
# Проверить версию Node.js (должна быть >= 20.0.0)
node --version

# Проверить версию npm (должна быть >= 10.0.0)
npm --version

# Проверить Docker
docker --version
docker-compose --version

# Проверить Git
git --version
```

Если что-то не установлено:
- **Node.js**: https://nodejs.org/ (скачайте LTS версию)
- **Docker Desktop**: https://www.docker.com/products/docker-desktop/
- **Git**: https://git-scm.com/downloads

## Шаг 2: Копирование файлов

1. Создайте папку для проекта:
```bash
mkdir ~/stankoff-portal
cd ~/stankoff-portal
```

2. Скопируйте все файлы проекта в эту папку

3. Проверьте структуру:
```bash
ls -la
# Должны увидеть:
# - apps/
# - docker-compose.yml
# - package.json
# - .env.example
# - README.md
```

## Шаг 3: Установка зависимостей

```bash
# В корневой директории проекта
npm install

# Установить зависимости для frontend
cd apps/frontend
npm install

# Установить зависимости для backend
cd ../backend
npm install

# Вернуться в корень
cd ../..
```

⏱️ Это займёт 3-5 минут в зависимости от скорости интернета.

## Шаг 4: Настройка переменных окружения

```bash
# Создать .env файл из примера
cp .env.example .env
```

Откройте `.env` в текстовом редакторе и настройте:

```env
# Database - эти настройки уже правильные для Docker
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=stankoff_portal
DATABASE_USER=stankoff_admin
DATABASE_PASSWORD=stankoff_secret_2026

# Backend - можно оставить как есть
BACKEND_PORT=3001
NODE_ENV=development
JWT_SECRET=change-this-to-random-string-in-production

# Frontend - можно оставить как есть
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Yandex S3 - ЗАПОЛНИТЕ СВОИ ДАННЫЕ
S3_ENDPOINT=https://storage.yandexcloud.net
S3_REGION=ru-central1
S3_ACCESS_KEY_ID=YOUR_ACCESS_KEY_HERE
S3_SECRET_ACCESS_KEY=YOUR_SECRET_KEY_HERE
S3_BUCKET_NAME=stankoff-portal-files
```

### Как получить ключи Yandex S3:

1. Зайдите в Яндекс.Облако: https://console.cloud.yandex.ru/
2. Перейдите в раздел "Object Storage"
3. Создайте бакет или используйте существующий
4. Создайте сервисный аккаунт и получите ключи доступа
5. Вставьте ключи в `.env`

## Шаг 5: Запуск PostgreSQL в Docker

```bash
# Запустить PostgreSQL и pgAdmin
npm run docker:up

# Дождаться запуска (может занять 30-60 секунд)
# Проверить что контейнеры запущены
docker ps
```

Вы должны увидеть два контейнера:
- `stankoff-postgres` (PostgreSQL)
- `stankoff-pgadmin` (pgAdmin)

### Проверка работы PostgreSQL:

Откройте pgAdmin: http://localhost:5050

Логин: `admin@stankoff.ru`
Пароль: `admin`

Добавьте сервер PostgreSQL:
- Host: `postgres` (или `host.docker.internal` на Mac)
- Port: `5432`
- Database: `stankoff_portal`
- Username: `stankoff_admin`
- Password: `stankoff_secret_2026`

## Шаг 6: Первый запуск приложения

Откройте ДВА терминала:

### Terminal 1 - Backend

```bash
cd ~/stankoff-portal
npm run dev:backend
```

Дождитесь сообщения:
```
🚀 Backend запущен на http://localhost:3001
📊 API доступен по адресу http://localhost:3001/api
```

### Terminal 2 - Frontend

```bash
cd ~/stankoff-portal
npm run dev:frontend
```

Дождитесь сообщения:
```
✓ Ready in 2.5s
○ Local:   http://localhost:3000
```

## Шаг 7: Открыть приложение

Откройте браузер и перейдите на http://localhost:3000

Вы должны увидеть корпоративный портал с:
- Шапкой сайта
- Боковым меню
- Канбан-доской с заявками

## 🎉 Готово!

Теперь можно начинать разработку.

---

## 🔧 Альтернативный запуск (в одном терминале)

Вместо двух терминалов можно запустить всё одной командой:

```bash
npm run dev
```

Эта команда запустит frontend и backend одновременно.

---

## 🐛 Решение проблем

### Проблема: "Cannot find module"

```bash
# Переустановите зависимости
rm -rf node_modules apps/*/node_modules
npm install
cd apps/frontend && npm install
cd ../backend && npm install
```

### Проблема: "Port 3000 already in use"

```bash
# Найти и убить процесс
lsof -ti:3000 | xargs kill -9

# Или изменить порт в package.json
# frontend: "dev": "next dev -p 3001"
```

### Проблема: "Port 5432 already in use"

У вас уже запущен PostgreSQL локально.

Вариант 1: Остановить локальный PostgreSQL
```bash
brew services stop postgresql@14  # на Mac
sudo service postgresql stop       # на Linux
```

Вариант 2: Изменить порт в docker-compose.yml
```yaml
ports:
  - "5433:5432"  # внешний порт 5433
```

И в .env:
```env
DATABASE_PORT=5433
```

### Проблема: "Cannot connect to database"

1. Проверьте что Docker контейнер запущен:
```bash
docker ps
```

2. Проверьте логи:
```bash
npm run docker:logs
```

3. Перезапустите контейнеры:
```bash
npm run docker:down
npm run docker:up
```

### Проблема: Frontend не подключается к Backend

1. Проверьте что backend запущен: http://localhost:3001/api
2. Проверьте переменные окружения в `.env`
3. Очистите кэш Next.js:
```bash
cd apps/frontend
rm -rf .next
npm run dev
```

---

## 📝 Следующие шаги

1. **Изучить код**
   - Frontend: `apps/frontend/src/`
   - Backend: `apps/backend/src/`

2. **Прочитать документацию**
   - README.md - общая информация
   - Комментарии в коде

3. **Начать разработку**
   - Добавить новые компоненты
   - Создать API endpoints
   - Подключить WebSocket

4. **Настроить IDE**
   - Установить расширения для TypeScript
   - Настроить ESLint и Prettier
   - Включить автоформатирование

---

## 💡 Полезные команды

```bash
# Посмотреть логи Docker
npm run docker:logs

# Остановить все контейнеры
npm run docker:down

# Перезапустить контейнеры
npm run docker:down && npm run docker:up

# Проверить типы TypeScript
cd apps/frontend && npm run type-check
cd apps/backend && npm run build

# Очистить всё и начать заново
rm -rf node_modules apps/*/node_modules apps/*/.next apps/*/dist
npm install
cd apps/frontend && npm install
cd ../backend && npm install
```

---

## 🆘 Нужна помощь?

Если что-то не работает:

1. Проверьте логи в терминале
2. Посмотрите раздел "Решение проблем" выше
3. Проверьте версии Node.js, npm, Docker
4. Убедитесь что все порты свободны (3000, 3001, 5432, 5050)
5. Напишите на support@stankoff.ru

---

Удачи в разработке! 🚀
